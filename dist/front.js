"use strict";
/*const pantalla = document.getElementById("pantalla");
if (pantalla) {
    console.log(pantalla.textContent);
    
    const botones = document.querySelectorAll("button");
    if (botones) {
        botones.forEach(boton => {
            boton.addEventListener('click', () => {
                const valorBoton = boton.textContent.trim();
                 let operandoA: number = 0;
                 let operandoB: number = 0;
                 let operador: number = 0;
                 let resultado: number = 0;
                if (boton.id === 'c') {
                    pantalla.textContent = '0';
                    return;
                }
                if (boton.id === 'borrar') {
                     if (pantalla.textContent.length === 1 || pantalla.textContent === 'ERROR') {
                        pantalla.textContent = '0';
                     }else{
                        pantalla.textContent = pantalla.textContent.slice(0, -1);
                    }
                    return;
                }

                if (boton.id === 'resultado') {
                    //immprime la repuesta del back
                } else {
                // si por alguna razon no hay resultado lanzamos error
                }
                
                
               // const operaciones = ['suma', 'resta', 'multiplicar', 'dividir'];
              pantalla.textContent+= boton.id
                
                
                
            });
        });
    }
}

*/
Object.defineProperty(exports, "__esModule", { value: true });
//# sourceMappingURL=front.js.map